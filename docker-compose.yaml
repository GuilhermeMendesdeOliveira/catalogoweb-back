services:
  api:
    build: .
    container_name: api_catalogocamafeu
    ports:
      - "54850:54850"
    environment:
      - DATABASE_URL=${POSTGRES_URL}
      - NODE_ENV=development
    depends_on:
      - postgres
    dns:
      - 8.8.8.8
      - 1.1.1.1
volumes:
  pgdata:
